# XcodeGen project for the Listed app target.
#
# Build with:
#   brew install xcodegen
#   xcodegen generate
#   open Listed.xcodeproj
#
# The Swift package at the repo root provides ListedCore + ListedUI; this project
# pulls them in as a local package dependency.

name: Listed
options:
  bundleIdPrefix: app.listed
  deploymentTarget:
    macOS: "26.0"
    iOS: "26.0"
  groupSortPosition: top
  createIntermediateGroups: true

settings:
  base:
    SWIFT_VERSION: "6.0"
    DEVELOPMENT_TEAM: ""
    MARKETING_VERSION: "2.1.0"
    CURRENT_PROJECT_VERSION: "1"
    SWIFT_STRICT_CONCURRENCY: "complete"
    ENABLE_USER_SCRIPT_SANDBOXING: "YES"

packages:
  Listed:
    path: .

targets:
  Listed (macOS):
    type: application
    platform: macOS
    deploymentTarget: "26.0"
    sources:
      - path: App/ListedApp
        excludes:
          - "Info.plist"
          - "Info.macOS.plist"
          - "Info.iOS.plist"
          - "*.entitlements"
    settings:
      base:
        PRODUCT_NAME: Listed
        PRODUCT_BUNDLE_IDENTIFIER: app.listed.Listed
        INFOPLIST_FILE: App/ListedApp/Info.macOS.plist
        GENERATE_INFOPLIST_FILE: "NO"
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
        CODE_SIGN_ENTITLEMENTS: App/ListedApp/Listed.macOS.entitlements
        ENABLE_HARDENED_RUNTIME: "YES"
        COMBINE_HIDPI_IMAGES: "YES"
        LD_RUNPATH_SEARCH_PATHS: "$(inherited) @executable_path/../Frameworks"
    dependencies:
      - package: Listed
        product: ListedCore
      - package: Listed
        product: ListedUI

  Listed (iOS):
    type: application
    platform: iOS
    deploymentTarget: "26.0"
    sources:
      - path: App/ListedApp
        excludes:
          - "Info.plist"
          - "Info.macOS.plist"
          - "Info.iOS.plist"
          - "*.entitlements"
    settings:
      base:
        PRODUCT_NAME: Listed
        PRODUCT_BUNDLE_IDENTIFIER: app.listed.Listed
        INFOPLIST_FILE: App/ListedApp/Info.iOS.plist
        GENERATE_INFOPLIST_FILE: "NO"
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
        CODE_SIGN_ENTITLEMENTS: App/ListedApp/Listed.iOS.entitlements
        TARGETED_DEVICE_FAMILY: "1,2"
        SUPPORTS_MACCATALYST: "NO"
        # Disable the iOS-on-Mac compatibility runtime ("Designed for iPad on
        # Mac"). Without this, both the macOS bundle and the iOS bundle share
        # the same bundle ID at the LaunchServices level, and macOS will
        # arbitrarily launch one of them — usually the iPad one, with the
        # "Designed for iPad" badge. Real iPad device support stays intact via
        # TARGETED_DEVICE_FAMILY: "1,2" above.
        SUPPORTS_MAC_DESIGNED_FOR_IPHONE_IPAD: "NO"
        LD_RUNPATH_SEARCH_PATHS: "$(inherited) @executable_path/Frameworks"
    dependencies:
      - package: Listed
        product: ListedCore
      - package: Listed
        product: ListedUI

  ListedCoreTests:
    type: bundle.unit-test
    platform: macOS
    deploymentTarget: "26.0"
    sources:
      - path: Tests/ListedCoreTests
    dependencies:
      - package: Listed
        product: ListedCore

schemes:
  Listed (macOS):
    build:
      targets:
        Listed (macOS): all
    run:
      config: Debug
    test:
      config: Debug
      targets:
        - ListedCoreTests
  Listed (iOS):
    build:
      targets:
        Listed (iOS): all
    run:
      config: Debug
